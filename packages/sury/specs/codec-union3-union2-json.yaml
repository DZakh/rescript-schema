# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number, S.bigint]).with(S.to, S.union([S.json, S.bigint]))
  input: string | number | bigint
  output: bigint | JSON
  instantiations: 6226
jsonSchema:
  input: Expected JSON, received string | number | bigint
  output: Expected JSON, received JSON | bigint
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from bigint to JSON | bigint: the bigint arm matches as it is. Use S.to for a coder, or S.never to drop it"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Unsupported conversion from JSON | bigint to string | number | bigint: nothing converts to JSON. Use S.to for a coder"
