# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number, S.bigint]).with(S.to, S.union([S.json, S.bigint]))
  input: string | number | bigint
  output: bigint | JSON
  instantiations: 6226
jsonSchema:
  input: Expected JSON, received string | number | bigint
  output: Expected JSON, received JSON | bigint
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from bigint to JSON | bigint. Use S.to(from, to, {decode, encode}), or S.never on an arm"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode JSON | bigint to string | number | bigint. Use S.to to define a custom decoder"
