# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number, S.bigint]).with(S.to, S.union([S.json, S.bigint]))
  input: string | number | bigint
  output: bigint | JSON
  instantiations: 6226
jsonSchema:
  input: Expected JSON, received string | number | bigint
  output: Expected JSON, received JSON | bigint
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from bigint to JSON | bigint: bigint has the same type as the source and the others don't. Use S.to on that arm, or S.never to mark it unreachable"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't convert JSON | bigint to string | number | bigint: JSON has no same-type variant on the other side. Use S.to on that arm, or S.never to mark it unreachable"
