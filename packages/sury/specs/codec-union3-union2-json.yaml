# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number, S.bigint]).with(S.to, S.union([S.json, S.bigint]))
  input: string | number | bigint
  output: bigint | JSON
  instantiations: 6226
jsonSchema:
  input: Expected JSON, received string | number | bigint
  output: Expected JSON, received JSON | bigint
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous bigint -> JSON | bigint. Should JSON be decoded or ignored? Choose with S.to for bigint -> JSON, or S.never -> JSON"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous JSON | bigint -> string | number | bigint. Should JSON be decoded or ignored? Choose with S.to for JSON -> string, or S.never -> JSON"
