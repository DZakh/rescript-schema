# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.jsonString.with(S.to, S.schema({ a: S.string.with(S.to, S.uint8Array) }))"
  input: string
  output: "{ a: Uint8Array<ArrayBufferLike>; }"
  instantiations: 5707
jsonSchema:
  input: '{ type: "string", contentMediaType: "application/json" }'
  output: "Failed at a: Expected JSON, received Uint8Array"
  draft-2020-12:
    input: '{ type: "string", contentMediaType: "application/json", contentSchema: { type: "object", properties: { a: { type: "string" } }, required: ["a"] }, $schema: "https://json-schema.org/draft/2020-12/schema" }'
  openapi-3.0:
    input: '{ type: "string" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[4](i);let v0;try{v0=JSON.parse(i)}catch(t){e[0](i)}typeof v0==="object"&&v0&&!Array.isArray(v0)||e[3](v0);let v1=v0["a"];typeof v1==="string"||e[2](v1);return {a:e[1](v1)}}
    examples:
      present:
        input: '"{\"a\":\"€\"}"'
        output: "{ a: new Uint8Array([226, 130, 172]) }"
      missing-a:
        input: '"{}"'
        error: "Failed at a: Expected string, received undefined"
  decode:
    expression: i=>{let v0;try{v0=JSON.parse(i)}catch(t){e[0](i)}typeof v0==="object"&&v0&&!Array.isArray(v0)||e[3](v0);let v1=v0["a"];typeof v1==="string"||e[2](v1);return {a:e[1](v1)}}
    examples:
      present:
        input: '"{\"a\":\"€\"}"'
        output: "{ a: new Uint8Array([226, 130, 172]) }"
  encode:
    expression: i=>{let v0=i["a"];let v1=e[0].decode(v0);return "{\"a\":"+e[1](v1)+"}"}
    examples:
      present:
        input: "{ a: new Uint8Array([226, 130, 172]) }"
        output: '"{\"a\":\"€\"}"'
