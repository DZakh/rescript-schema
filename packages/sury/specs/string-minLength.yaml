# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.minLength, 2)
  input: string
  output: string
  instantiations: 789
jsonSchema:
  input: '{ type: "string", minLength: 2 }'
  output: '{ type: "string", minLength: 2 }'
vs:
  zod: z.string().min(2)
operations:
  parse:
    expression: i=>{typeof i==="string"||e[2](i);i.length>3||e[0](i)>1||e[1](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      two-astral-code-points:
        input: '"😀😀"'
        output: '"😀😀"'
      one-astral-code-point-two-units:
        input: '"😀"'
        error: Expected string.length >= 2, received "😀"
      lone-surrogate-pair-halves:
        input: '"\ud83d\ud83d"'
        output: '"\ud83d\ud83d"'
      longer:
        input: '"abc"'
        output: '"abc"'
      shorter:
        input: '"a"'
        error: Expected string.length >= 2, received "a"
      empty:
        input: '""'
        error: Expected string.length >= 2, received ""
      single-space:
        input: '" "'
        error: Expected string.length >= 2, received " "
      wrong-type:
        input: "1"
        error: Expected string.length >= 2, received 1
      wrong-type-null:
        input: "null"
        error: Expected string.length >= 2, received null
  decode:
    expression: i=>{i.length>3||e[0](i)>1||e[1](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      two-astral-code-points:
        input: '"😀😀"'
        output: '"😀😀"'
      one-astral-code-point-two-units:
        input: '"😀"'
        error: Expected string.length >= 2, received "😀"
      lone-surrogate-pair-halves:
        input: '"\ud83d\ud83d"'
        output: '"\ud83d\ud83d"'
      shorter:
        input: '"a"'
        error: Expected string.length >= 2, received "a"
      empty:
        input: '""'
        error: Expected string.length >= 2, received ""
  encode:
    expression: i=>{i.length>3||e[0](i)>1||e[1](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      two-astral-code-points:
        input: '"😀😀"'
        output: '"😀😀"'
      one-astral-code-point-two-units:
        input: '"😀"'
        error: Expected string.length >= 2, received "😀"
      lone-surrogate-pair-halves:
        input: '"\ud83d\ud83d"'
        output: '"\ud83d\ud83d"'
      shorter:
        input: '"a"'
        error: Expected string.length >= 2, received "a"
      empty:
        input: '""'
        error: Expected string.length >= 2, received ""
