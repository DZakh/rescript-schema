# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchemaOrThrow({ type: "string", minLength: 100, maxLength: 50 })'
  input: never
  output: never
  instantiations: 828
jsonSchema:
  input: "{ not: {} }"
  output: "{ not: {} }"
isEqual: (a,b)=>a===b||e[0](a,b)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{e[0](i);return i}
    examples:
      rejects:
        input: '"anything"'
        error: Expected never, received "anything"
  decode: eq-to-parse
  encode: eq-to-parse
