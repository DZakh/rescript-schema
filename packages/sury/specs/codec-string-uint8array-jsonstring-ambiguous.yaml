# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.uint8Array).with(S.to, S.jsonString)
  input: string
  output: string
  instantiations: 4699
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "string", contentMediaType: "application/json" }'
  openapi-3.0:
    output: '{ type: "string" }'
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous Uint8Array -> JSON string. Should the bytes be packed or unpacked? Choose with S.to and "pack" or "unpack"'
  decode: eq-to-parse
  encode:
    creationError: 'SuryError: Ambiguous JSON string -> Uint8Array. Should the bytes be packed or unpacked? Choose with S.to and "pack" or "unpack"'
