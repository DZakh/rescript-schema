# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.json.with(S.to, S.union([S.bigint, S.string]))
  input: JSON
  output: string | bigint
  instantiations: 5394
jsonSchema:
  input: '{ type: "string" }'
  fromInputType: string
  output: Expected JSON, received bigint | string
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{for(;;){let r;if(typeof i==="string"){try{let v0;try{v0=BigInt(i)}catch(_){e[0](i)}v0||i.trim()||e[0](i);i=v0;break}catch(x){(r||(r=[])).push(e[1](x))}break}e[2](i,...(r||[]))}return i}
    examples:
      bigint-string:
        input: '"123"'
        output: 123n
      catchall-string:
        input: '"abc"'
        output: '"abc"'
      no-double-decoding:
        input: "true"
        error: Expected bigint | string, received true
      number:
        input: "123"
        error: Expected bigint | string, received 123
      negative-bigint-string:
        input: '"-123"'
        output: -123n
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){if(typeof i==="bigint"){i=""+i;break}if(typeof i==="string")break;e[0](i)}return i}
    examples:
      bigint:
        input: 123n
        output: '"123"'
      string:
        input: '"abc"'
        output: '"abc"'
      numeric-string:
        input: '"123"'
        output: '"123"'
