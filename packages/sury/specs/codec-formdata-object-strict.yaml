# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.formData.with(S.to, S.schema({ a: S.string }).with(S.strict))"
  input: FormData
  output: "{ a: string; }"
  instantiations: 5671
jsonSchema:
  input: Expected JSON, received FormData
  output: '{ type: "object", properties: { a: { type: "string" } }, additionalProperties: false, required: ["a"] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: S.strict is not supported by S.formData. Use S.strip"
  decode: eq-to-parse
  encode: eq-to-parse
