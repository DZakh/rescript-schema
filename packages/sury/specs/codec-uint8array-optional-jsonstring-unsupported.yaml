# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.optional(S.jsonString))
  input: Uint8Array<ArrayBufferLike>
  output: string | undefined
  instantiations: 4620
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: Expected JSON, received JSON string | undefined
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode Uint8Array -> JSON string | undefined. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode JSON string | undefined -> Uint8Array. Define custom codec with S.to"
