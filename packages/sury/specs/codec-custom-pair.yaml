# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.string.with(S.to, S.number.with(S.gt, 0), { decode: (value) => value.length, encode: (value) => "x".repeat(value) })'
  input: string
  output: number
  instantiations: 4993
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "number", exclusiveMinimum: 0 }'
  openapi-3.0:
    output: '{ type: "number", minimum: 0, exclusiveMinimum: true }'
vs:
  zod: 'z.codec(z.string(), z.number().positive(), { decode: (value) => value.length, encode: (value) => "x".repeat(value) })'
operations:
  parse:
    expression: i=>{typeof i==="string"||e[4](i);let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="number"&&v0===v0||e[3](v0);v0>0||e[2](v0);return v0}
    examples:
      valid:
        input: '"abc"'
        output: "3"
      target-refinement-runs-on-decode-result:
        input: '""'
        error: Expected number > 0, received 0
      invalid-type:
        input: "123"
        error: Expected string, received 123
  decode:
    expression: i=>{let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="number"&&v0===v0||e[3](v0);v0>0||e[2](v0);return v0}
    examples:
      valid:
        input: '"abc"'
        output: "3"
  encode:
    expression: i=>{i>0||e[3](i);let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="string"||e[2](v0);return v0}
    examples:
      round-trip:
        input: "3"
        output: '"xxx"'
      rejects-invalid-target-value:
        input: "0"
        error: Expected number > 0, received 0
