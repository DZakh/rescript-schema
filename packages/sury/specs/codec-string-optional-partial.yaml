# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.optional(S.string))
  input: string
  output: string | undefined
  instantiations: 4599
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received string | undefined
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous string -> string | undefined. Should undefined be decoded or ignored? Choose with S.to for string -> undefined, or S.never -> undefined"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous string | undefined -> string. Should undefined be decoded or ignored? Choose with S.to for undefined -> string, or S.never -> undefined"
