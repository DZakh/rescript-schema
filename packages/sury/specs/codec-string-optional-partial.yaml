# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.optional(S.string))
  input: string
  output: string | undefined
  instantiations: 4599
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received string | undefined
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from string to string | undefined. Use S.to(from, to, {decode, encode}), or S.never on an arm"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous conversion from string | undefined to string. Use S.to(from, to, {decode, encode}), or S.never on an arm"
