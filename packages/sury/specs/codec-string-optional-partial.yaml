# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.optional(S.string))
  input: string
  output: string | undefined
  instantiations: 4599
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received string | undefined
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from string to string | undefined: string has the same type as the source and the others don't. Use S.to on that arm, or S.never to mark it unreachable"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous conversion from string | undefined to string: string has the same type as the target and the others don't. Use S.to on that arm, or S.never to mark it unreachable"
