# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.optional(S.string))
  input: string
  output: string | undefined
  instantiations: 4599
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received string | undefined
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from string to string | undefined: the string arm matches as it is. Use S.to for a coder, or S.never to drop it"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous conversion from string | undefined to string: the string arm matches as it is. Use S.to for a coder, or S.never to drop it"
