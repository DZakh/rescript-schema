# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.record(S.string).with(S.to, S.schema({ a: S.optional(S.string) }))"
  input: "{ [x: string]: string; }"
  output: "{ a?: string | undefined; }"
  instantiations: 6342
jsonSchema:
  input: '{ type: "object", additionalProperties: { type: "string" } }'
  output: '{ type: "object", properties: { a: { type: "string" } } }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Failed at a: Ambiguous conversion from string to string | undefined: the string arm matches as it is. Use S.to for a coder, or S.never to drop it"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Failed at a: Ambiguous conversion from string | undefined to string: the string arm matches as it is. Use S.to for a coder, or S.never to drop it"
