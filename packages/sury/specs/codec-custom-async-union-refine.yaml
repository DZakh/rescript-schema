# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.union([S.string.with(S.to, S.number, { decode: { async: (value) => Promise.resolve(value.length) }, encode: (value) => "x".repeat(value) }), S.number]).with(S.refine, (value) => value > 1, { error: "Expected more than 1" })'
  input: string | number
  output: number
  instantiations: 5805
jsonSchema:
  input: '{ anyOf: [{ type: "string" }, { type: "number" }] }'
  output: '{ type: "number" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    isAsync: true
    expression: i=>{try{for(;;){if(typeof i==="string"){let v0=e[0](i);i=v0.then(v0=>{typeof v0==="number"&&v0===v0||e[1](v0);return v0});break}if(typeof i==="number"&&i===i)break;e[2](i)}let v1=Promise.resolve(i);return v1.then(v1=>{e[3](v1)||e[4](v1);return v1})}catch(v2){return Promise.reject(v2)}}
    examples:
      custom-arm:
        input: '"abc"'
        output: "3"
      custom-arm-rejected-after-await:
        input: '"a"'
        error: Expected more than 1
      sibling-arm:
        input: "5"
        output: "5"
      sibling-arm-rejected:
        input: "1"
        error: Expected more than 1
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){let r;if(typeof i==="number"&&i===i){try{let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="string"||e[2](v0);i=v0;break}catch(x){(r||(r=[])).push(e[3](x))}break}e[4](i,...(r||[]))}e[5](i)||e[6](i);return i}
    examples:
      # FIXME: the reversed union applies the refine after its member's encode,
      # so the predicate sees "xxx" where the value it was written for is 3.
      # Same with a sync member; unrelated to the async continuation.
      custom-arm:
        input: "3"
        error: Expected more than 1
