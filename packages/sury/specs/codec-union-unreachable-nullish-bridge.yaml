# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.never.with(S.to, S.schema(null)), S.string]).with(S.to, S.union([S.schema(undefined), S.string]))
  input: string
  output: string | undefined
  instantiations: 6456
jsonSchema:
  input: '{ anyOf: [{ not: {} }, { type: "string" }] }'
  output: Expected JSON, received undefined | string
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Unsupported conversion from never | string to undefined | string: nothing converts to undefined. Use S.to for a coder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Unsupported conversion from undefined | string to null | string: nothing converts to undefined. Use S.to for a coder"
