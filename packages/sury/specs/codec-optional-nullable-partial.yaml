# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.string).with(S.to, S.nullable(S.boolean))
  input: string | undefined
  output: boolean | null
  instantiations: 4786
jsonSchema:
  input: Expected JSON, received string | undefined
  output: '{ anyOf: [{ type: "boolean" }, { type: "null" }] }'
  openapi-3.0:
    output: '{ type: "boolean", nullable: true }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Unsupported conversion from string | undefined to boolean | null: nothing converts to string. Use S.to for a coder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Unsupported conversion from boolean | null to string | undefined: nothing converts to boolean. Use S.to for a coder"
