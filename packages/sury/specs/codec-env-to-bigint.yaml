# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.bigint)
  input: string
  output: bigint
  instantiations: 4419
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received bigint
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[2](i);typeof i==="string"||e[0](i);let v0;try{v0=BigInt(i)}catch(_){e[1](i)}v0||i.trim()||e[1](i);return v0}
    examples:
      valid:
        input: '"42"'
        output: 42n
      blank:
        input: '""'
        error: Expected bigint, received ""
      not-a-bigint:
        input: '"x"'
        error: Expected bigint, received "x"
  decode:
    expression: i=>{typeof i==="string"||e[0](i);let v0;try{v0=BigInt(i)}catch(_){e[1](i)}v0||i.trim()||e[1](i);return v0}
    examples:
      valid:
        input: '"42"'
        output: 42n
  encode:
    expression: i=>{return ""+i}
    examples:
      valid:
        input: 42n
        output: '"42"'
