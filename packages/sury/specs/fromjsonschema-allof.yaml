# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchemaOrThrow({ allOf: [{ enum: ["a", "b"] }, { enum: ["b", "c"] }] })'
  input: '"b"'
  output: '"b"'
  instantiations: 1031
jsonSchema:
  input: '{ allOf: [{ enum: ["a", "b"] }, { enum: ["b", "c"] }] }'
  output: '{ allOf: [{ enum: ["a", "b"] }, { enum: ["b", "c"] }] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{e[0](i);e[1](i)||e[2](i);return i}
    examples:
      in-both-members:
        input: '"b"'
        output: '"b"'
      in-first-member-only:
        input: '"a"'
        error: Should pass for all schemas of the allOf property.
      in-neither-member:
        input: '"z"'
        error: Should pass for all schemas of the allOf property.
  decode:
    expression: i=>{e[0](i)||e[1](i);return i}
    examples:
      in-both-members:
        input: '"b"'
        output: '"b"'
  encode:
    expression: i=>{e[0](i)||e[1](i);return i}
    examples:
      in-both-members:
        input: '"b"'
        output: '"b"'
