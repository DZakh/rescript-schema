# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.maxLength, 1)
  input: string
  output: string
  instantiations: 577
jsonSchema:
  input: '{ type: "string", maxLength: 1 }'
  output: '{ type: "string", maxLength: 1 }'
vs:
  zod: z.string().max(1)
operations:
  parse:
    expression: i=>{typeof i==="string"||e[1](i);i.length<2||e[0](i);return i}
    examples:
      at-the-length:
        input: '"a"'
        output: '"a"'
      one-astral-code-point-two-units:
        input: '"😀"'
        error: Expected string.length <= 1, received "😀"
      two-code-points:
        input: '"ab"'
        error: Expected string.length <= 1, received "ab"
      empty:
        input: '""'
        output: '""'
      wrong-type:
        input: "1"
        error: Expected string.length <= 1, received 1
  decode:
    expression: i=>{i.length<2||e[0](i);return i}
    examples:
      at-the-length:
        input: '"a"'
        output: '"a"'
      one-astral-code-point-two-units:
        input: '"😀"'
        error: Expected string.length <= 1, received "😀"
      two-code-points:
        input: '"ab"'
        error: Expected string.length <= 1, received "ab"
      empty:
        input: '""'
        output: '""'
  encode:
    expression: i=>{i.length<2||e[0](i);return i}
    examples:
      at-the-length:
        input: '"a"'
        output: '"a"'
      one-astral-code-point-two-units:
        input: '"😀"'
        error: Expected string.length <= 1, received "😀"
      two-code-points:
        input: '"ab"'
        error: Expected string.length <= 1, received "ab"
      empty:
        input: '""'
        output: '""'
