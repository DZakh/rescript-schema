# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.record(S.string).with(S.to, S.jsonString)
  input: "{ [x: string]: string; }"
  output: string
  instantiations: 4541
jsonSchema:
  input: '{ type: "object", additionalProperties: { type: "string" } }'
  output: '{ type: "string", contentMediaType: "application/json" }'
  draft-2020-12:
    output: '{ type: "string", contentMediaType: "application/json", contentSchema: { type: "object", additionalProperties: { type: "string" } }, $schema: "https://json-schema.org/draft/2020-12/schema" }'
  openapi-3.0:
    output: '{ type: "string" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[1](i);for(let v0 in i){try{let v1=i[v0];typeof v1==="string"||e[0](v1);}catch(v2){v2.path=[v0,...v2.path];throw v2}}return JSON.stringify(i)}
    examples:
      valid:
        input: '{ a: "x", b: "say \"hi\"" }'
        output: '"{\"a\":\"x\",\"b\":\"say \\\"hi\\\"\"}"'
      invalid-value:
        input: "{ a: 1 }"
        error: "Failed at a: Expected string, received 1"
  decode:
    expression: i=>{return JSON.stringify(i)}
    examples:
      valid:
        input: '{ a: "x" }'
        output: '"{\"a\":\"x\"}"'
  encode:
    expression: i=>{let v0;try{v0=JSON.parse(i)}catch(t){e[0](i)}typeof v0==="object"&&v0&&!Array.isArray(v0)||e[2](v0);for(let v1 in v0){try{let v2=v0[v1];typeof v2==="string"||e[1](v2);}catch(v3){v3.path=[v1,...v3.path];throw v3}}return v0}
    examples:
      valid:
        input: '"{\"a\":\"x\"}"'
        output: '{ a: "x" }'
