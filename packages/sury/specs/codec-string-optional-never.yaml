# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.union([S.string, S.never.with(S.to, S.schema(undefined))]))
  input: string
  output: string | undefined
  instantiations: 5991
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "string" }'
  fromOutputType: string
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[0](i);return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
      undefined-string-stays-string:
        input: '"undefined"'
        output: '"undefined"'
  decode: identity
  encode:
    expression: i=>{for(;;){if(typeof i==="string")break;if(i===void 0){e[0](i);break}e[1](i)}return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
