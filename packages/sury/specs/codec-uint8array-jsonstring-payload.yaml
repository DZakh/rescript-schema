# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.uint8Array.with(S.to, S.jsonString.with(S.to, S.schema({ sub: S.string })))"
  input: Uint8Array<ArrayBufferLike>
  output: "{ sub: string; }"
  instantiations: 5815
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: '{ type: "object", properties: { sub: { type: "string" } }, required: ["sub"] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{i instanceof e[4]||e[5](i);let v0=e[0].decode(i);let v1;try{v1=JSON.parse(v0)}catch(t){e[1](v0)}typeof v1==="object"&&v1&&!Array.isArray(v1)||e[3](v1);let v2=v1["sub"];typeof v2==="string"||e[2](v2);return {sub:v2}}
    examples:
      claims:
        input: new Uint8Array([123, 34, 115, 117, 98, 34, 58, 34, 97, 34, 125])
        output: '{ sub: "a" }'
      missing-sub:
        input: new Uint8Array([123, 125])
        error: "Failed at sub: Expected string, received undefined"
      not-a-document:
        input: new Uint8Array([110, 111, 112, 101])
        error: Expected JSON string, received "nope"
  decode:
    expression: i=>{let v0=e[0].decode(i);let v1;try{v1=JSON.parse(v0)}catch(t){e[1](v0)}typeof v1==="object"&&v1&&!Array.isArray(v1)||e[3](v1);let v2=v1["sub"];typeof v2==="string"||e[2](v2);return {sub:v2}}
    examples:
      claims:
        input: new Uint8Array([123, 34, 115, 117, 98, 34, 58, 34, 97, 34, 125])
        output: '{ sub: "a" }'
  encode:
    expression: i=>{let v0="{\"sub\":"+e[0](i["sub"])+"}";return e[1](v0)}
    examples:
      claims:
        input: '{ sub: "a" }'
        output: new Uint8Array([123, 34, 115, 117, 98, 34, 58, 34, 97, 34, 125])
