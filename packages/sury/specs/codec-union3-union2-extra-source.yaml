# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number, S.boolean]).with(S.to, S.union([S.number, S.string]))
  input: string | number | boolean
  output: string | number
  instantiations: 5850
jsonSchema:
  input: '{ anyOf: [{ type: "string" }, { type: "number" }, { type: "boolean" }] }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Unsupported conversion from string | number | boolean to number | string: nothing converts to boolean. Use S.to for a coder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Unsupported conversion from number | string to string | number | boolean: nothing converts to boolean. Use S.to for a coder"
