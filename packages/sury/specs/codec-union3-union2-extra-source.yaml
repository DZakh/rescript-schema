# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.number, S.boolean]).with(S.to, S.union([S.number, S.string]))
  input: string | number | boolean
  output: string | number
  instantiations: 5850
jsonSchema:
  input: '{ anyOf: [{ type: "string" }, { type: "number" }, { type: "boolean" }] }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Invalid operation: can't convert string | number | boolean to number | string — boolean has no same-type variant on the other side. Use S.to to say what you mean, or S.never to mark a variant unreachable"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Invalid operation: can't convert number | string to string | number | boolean — boolean has no same-type variant on the other side. Use S.to to say what you mean, or S.never to mark a variant unreachable"
