{
  "type": "object",
  "properties": {
    "ts": {
      "type": "object",
      "properties": {
        "schema": {
          "type": "string",
          "description": "The schema under test, as JS `.with`-chain source (e.g. `S.string.with(S.minLength, 3)`). You write this by hand; `spec check --write` never touches it."
        },
        "aliases": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Alternate `.with`-chain sources that must produce a schema equivalent to `schema` - same ts.input/ts.output, jsonSchema, and operations. Checked live (not separately snapshotted) by `spec check`. You write these by hand."
        },
        "input": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "`S.Input<typeof schema>` as a TS type string. Filled by `spec check --write`."
        },
        "output": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "`S.Output<typeof schema>` as a TS type string. Filled by `spec check --write`."
        },
        "instantiations": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "TS type-instantiation cost of this schema. Filled by `spec check --write`."
        }
      },
      "additionalProperties": false,
      "required": [
        "schema",
        "input",
        "output",
        "instantiations"
      ],
      "description": "The JS `.with`-chain surface: the schema itself, plus its inferred types and instantiation cost."
    },
    "jsonSchema": {
      "type": "object",
      "properties": {
        "input": {
          "type": "string",
          "description": "S.inputJSONSchema(schema), as source text, or its conversion error."
        },
        "fromInputType": {
          "type": "string",
          "description": "The type inferred by S.fromJSONSchema(input), only when it differs from ts.input; omit when equal or when input is a conversion error."
        },
        "output": {
          "type": "string",
          "description": "S.outputJSONSchema(schema), as source text, or its conversion error."
        },
        "fromOutputType": {
          "type": "string",
          "description": "The type inferred by S.fromJSONSchema(output), only when it differs from ts.output; omit when equal or when output is a conversion error."
        },
        "draft-2020-12": {
          "type": "object",
          "properties": {
            "input": {
              "type": "string",
              "description": "S.toJSONSchema(schema, { target }), as source text, or its conversion error. Only when this side differs from the default after ignoring $schema."
            },
            "fromInputType": {
              "type": "string",
              "description": "The type inferred by S.fromJSONSchema of this dialect's input document, only when it differs from ts.input; omit when equal or when input is a conversion error."
            },
            "output": {
              "type": "string",
              "description": "S.toJSONSchema(S.reverse(schema), { target }), as source text, or its conversion error. Only when this side differs from the default after ignoring $schema."
            },
            "fromOutputType": {
              "type": "string",
              "description": "The type inferred by S.fromJSONSchema of this dialect's output document, only when it differs from ts.output; omit when equal or when output is a conversion error."
            }
          },
          "additionalProperties": false,
          "description": "Sparse fields that differ from the default no-options emit after ignoring $schema. Omitted when nothing differs. Filled by `spec check --write`."
        },
        "openapi-3.0": {
          "type": "object",
          "properties": {
            "input": {
              "type": "string",
              "description": "S.toJSONSchema(schema, { target }), as source text, or its conversion error. Only when this side differs from the default after ignoring $schema."
            },
            "fromInputType": {
              "type": "string",
              "description": "The type inferred by S.fromJSONSchema of this dialect's input document, only when it differs from ts.input; omit when equal or when input is a conversion error."
            },
            "output": {
              "type": "string",
              "description": "S.toJSONSchema(S.reverse(schema), { target }), as source text, or its conversion error. Only when this side differs from the default after ignoring $schema."
            },
            "fromOutputType": {
              "type": "string",
              "description": "The type inferred by S.fromJSONSchema of this dialect's output document, only when it differs from ts.output; omit when equal or when output is a conversion error."
            }
          },
          "additionalProperties": false,
          "description": "Sparse fields that differ from the default no-options emit after ignoring $schema. Omitted when nothing differs. Filled by `spec check --write`."
        }
      },
      "additionalProperties": false,
      "required": [
        "input",
        "output"
      ],
      "description": "The JSON Schema of both directions, as one-line source text, plus any divergent output type inferred by S.fromJSONSchema for each generated document. Matching types are omitted; if a direction can't be represented, no round-trip type is recorded. Dialect keys record only the fields that differ from this default after ignoring $schema. Filled by `spec check --write`."
    },
    "vs": {
      "type": "object",
      "properties": {
        "zod": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "Zod (v4) schema whose type differs from ts on ≥1 side, e.g. `z.object({...})`."
                },
                "divergence": {
                  "type": "string",
                  "description": "How the Zod type differs from Sury's, and why. Hand-written."
                },
                "input": {
                  "type": "string",
                  "description": "Zod's input type, only if it diverges from ts.input (filled by `--write`); omit when equal."
                },
                "output": {
                  "type": "string",
                  "description": "Zod's output type, only if it diverges from ts.output (filled by `--write`); omit when equal."
                }
              },
              "additionalProperties": false,
              "required": [
                "schema",
                "divergence"
              ],
              "description": "Zod equivalent that infers a different type than Sury; divergent side(s) recorded."
            },
            {
              "type": "object",
              "properties": {
                "_skip": {
                  "type": "string",
                  "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                }
              },
              "additionalProperties": false,
              "required": [
                "_skip"
              ],
              "description": "Explicit opt-out for an unasserted dimension."
            }
          ],
          "description": "Equivalent Zod (v4) schema. Bare string: inferred types must equal ts.input/ts.output. Object `{schema,divergence,input?,output?}`: differs from ts - divergent side recorded, matching side omitted. `_skip` if Zod can't express it."
        }
      },
      "additionalProperties": false,
      "required": [
        "zod"
      ],
      "description": "Cross-library equivalents, type-checked against this spec."
    },
    "operations": {
      "type": "object",
      "properties": {
        "parse": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "object",
              "properties": {
                "isAsync": {
                  "type": "boolean",
                  "const": true,
                  "description": "`true` if this direction is async (built with S.asyncParser/asyncDecoder/asyncEncoder, examples awaited). Written when the block is first created; `spec check` errors when it disagrees with the schema. Omit when sync."
                },
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for an unasserted dimension."
                    }
                  ],
                  "description": "Compiled function source (`.toString()`). Filled by `spec check --write`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "output": {
                            "type": "string",
                            "description": "Expected output source text. Filled by `spec check --write`."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `spec check --write`."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "A named example: input plus expected output or error."
                  },
                  "description": "Named example cases, keyed by a short name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "Compiled codegen plus its runnable examples."
            },
            {
              "type": "object",
              "properties": {
                "creationError": {
                  "type": "string",
                  "description": "The Sury error thrown when this operation is compiled (a conversion rejected at operation creation). Filled by `spec check --write`."
                }
              },
              "additionalProperties": false,
              "required": [
                "creationError"
              ],
              "description": "An operation that can't be compiled - the creation-time error, ratcheted like codegen."
            }
          ],
          "description": "unknown → Output, with validation (the untrusted-input direction)."
        },
        "decode": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "string",
              "const": "eq-to-parse"
            },
            {
              "type": "object",
              "properties": {
                "isAsync": {
                  "type": "boolean",
                  "const": true,
                  "description": "`true` if this direction is async (built with S.asyncParser/asyncDecoder/asyncEncoder, examples awaited). Written when the block is first created; `spec check` errors when it disagrees with the schema. Omit when sync."
                },
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for an unasserted dimension."
                    }
                  ],
                  "description": "Compiled function source (`.toString()`). Filled by `spec check --write`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "output": {
                            "type": "string",
                            "description": "Expected output source text. Filled by `spec check --write`."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `spec check --write`."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "A named example: input plus expected output or error."
                  },
                  "description": "Named example cases, keyed by a short name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "Compiled codegen plus its runnable examples."
            },
            {
              "type": "object",
              "properties": {
                "creationError": {
                  "type": "string",
                  "description": "The Sury error thrown when this operation is compiled (a conversion rejected at operation creation). Filled by `spec check --write`."
                }
              },
              "additionalProperties": false,
              "required": [
                "creationError"
              ],
              "description": "An operation that can't be compiled - the creation-time error, ratcheted like codegen."
            }
          ],
          "description": "Input → Output, no top-level type narrowing (input is already typed)."
        },
        "encode": {
          "anyOf": [
            {
              "type": "string",
              "const": "identity"
            },
            {
              "type": "string",
              "const": "eq-to-parse"
            },
            {
              "type": "object",
              "properties": {
                "isAsync": {
                  "type": "boolean",
                  "const": true,
                  "description": "`true` if this direction is async (built with S.asyncParser/asyncDecoder/asyncEncoder, examples awaited). Written when the block is first created; `spec check` errors when it disagrees with the schema. Omit when sync."
                },
                "expression": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "_skip": {
                          "type": "string",
                          "description": "Why unasserted: parser-only | serializer-only | lossy | not-applicable | todo(#…)."
                        }
                      },
                      "additionalProperties": false,
                      "required": [
                        "_skip"
                      ],
                      "description": "Explicit opt-out for an unasserted dimension."
                    }
                  ],
                  "description": "Compiled function source (`.toString()`). Filled by `spec check --write`."
                },
                "examples": {
                  "type": "object",
                  "additionalProperties": {
                    "anyOf": [
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "output": {
                            "type": "string",
                            "description": "Expected output source text. Filled by `spec check --write`."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "output"
                        ]
                      },
                      {
                        "type": "object",
                        "properties": {
                          "input": {
                            "type": "string",
                            "description": "Source text for the input, e.g. '\"hello\"'. Hand-written; `spec check --write` fills output/error."
                          },
                          "error": {
                            "type": "string",
                            "description": "Expected error message. Filled by `spec check --write`."
                          }
                        },
                        "additionalProperties": false,
                        "required": [
                          "input",
                          "error"
                        ]
                      }
                    ],
                    "description": "A named example: input plus expected output or error."
                  },
                  "description": "Named example cases, keyed by a short name (e.g. `valid`, `invalid-type`)."
                }
              },
              "additionalProperties": false,
              "required": [
                "expression",
                "examples"
              ],
              "description": "Compiled codegen plus its runnable examples."
            },
            {
              "type": "object",
              "properties": {
                "creationError": {
                  "type": "string",
                  "description": "The Sury error thrown when this operation is compiled (a conversion rejected at operation creation). Filled by `spec check --write`."
                }
              },
              "additionalProperties": false,
              "required": [
                "creationError"
              ],
              "description": "An operation that can't be compiled - the creation-time error, ratcheted like codegen."
            }
          ],
          "description": "Output → Input (the reverse direction)."
        }
      },
      "additionalProperties": false,
      "required": [
        "parse",
        "decode",
        "encode"
      ],
      "description": "The three compiled directions through the schema: parse, decode, encode."
    }
  },
  "additionalProperties": false,
  "required": [
    "ts",
    "jsonSchema",
    "vs",
    "operations"
  ],
  "description": "One schema's complete, machine-checked contract. See the `spec` skill."
}
