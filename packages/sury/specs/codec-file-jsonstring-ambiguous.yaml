# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.file.with(S.to, S.jsonString)
  input: File
  output: string
  instantiations: 4431
jsonSchema:
  input: Expected JSON, received File
  output: '{ type: "string", contentMediaType: "application/json" }'
  openapi-3.0:
    output: '{ type: "string", format: "binary" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous conversion from File to JSON string. Use S.to(from, to, "unpack" | "pack")'
  decode: eq-to-parse
  encode: eq-to-parse
