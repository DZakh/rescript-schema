# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.file.with(S.to, S.jsonString)
  input: File
  output: string
  instantiations: 4564
jsonSchema:
  input: Expected JSON, received File
  output: '{ type: "string", contentMediaType: "application/json" }'
  openapi-3.0:
    output: '{ type: "string", format: "binary" }'
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous File -> JSON string. Should the bytes be packed or unpacked? Choose with S.to and "pack" or "unpack"'
  decode: eq-to-parse
  encode:
    creationError: 'SuryError: Ambiguous JSON string -> File. Should the bytes be packed or unpacked? Choose with S.to and "pack" or "unpack"'
