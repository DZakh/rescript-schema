# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.formData.with(S.to, S.schema({ a: S.string.with(S.trim) }))"
  input: FormData
  output: "{ a: string; }"
  instantiations: 5814
jsonSchema:
  input: Expected JSON, received FormData
  output: '{ type: "object", properties: { a: { type: "string" } }, required: ["a"] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Failed at a: Ambiguous "" for string. Should a blank input be rejected, kept, or read as absent? Choose with S.nonEmpty, S.minLength(0), or S.optional'
  decode: eq-to-parse
  encode:
    expression: i=>{let v0;try{v0=e[0](i["a"])}catch(x){e[1](x)}let v1=new e[2]();v1.append("a",v0);return v1}
    examples:
      built:
        input: '{ a: " x " }'
        output: ((f) => (f.append("a", "x"), f))(new FormData())
