# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.union([S.schema("apple"), S.string.with(S.to, S.number, { decode: Number, encode: String }), S.string])'
  input: string
  output: string | number
  instantiations: 5827
jsonSchema:
  input: '{ anyOf: [{ type: "string", const: "apple" }, { type: "string" }] }'
  output: '{ anyOf: [{ type: "string", const: "apple" }, { type: "number" }, { type: "string" }] }'
  openapi-3.0:
    input: '{ anyOf: [{ type: "string", enum: ["apple"] }, { type: "string" }] }'
    output: '{ anyOf: [{ type: "string", enum: ["apple"] }, { type: "number" }, { type: "string" }] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{for(;;){let r;if(typeof i==="string"&&i==="apple")break;if(typeof i==="string"){try{let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="number"&&v0===v0||e[2](v0);i=v0;break}catch(x){(r||(r=[])).push(e[3](x))}break}e[4](i,...(r||[]))}return i}
    examples:
      literal-claims-its-value:
        input: '"apple"'
        output: '"apple"'
      coerced-by-codec:
        input: '"42"'
        output: "42"
      unparseable-string-nan-falls-through-to-catch-all:
        input: '"cherry"'
        output: '"cherry"'
      wrong-tag:
        input: "42"
        error: Expected "apple" | string, received 42
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){if(typeof i==="number"&&i===i){let v0;try{v0=e[0](i)}catch(x){e[1](x)}typeof v0==="string"||e[2](v0);i=v0;break}if(typeof i==="string")break;e[3](i)}return i}
    examples:
      number-back-to-string:
        input: "42"
        output: '"42"'
      string-passes-through:
        input: '"apple"'
        output: '"apple"'
