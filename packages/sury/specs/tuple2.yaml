# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.schema(["bar", S.number])
  aliases:
    - S.tuple(["bar", S.number])
  input: '["bar", number]'
  output: '["bar", number]'
  instantiations: 967
jsonSchema:
  input: '{ type: "array", minItems: 2, maxItems: 2, items: [{ type: "string", const: "bar" }, { type: "number" }] }'
  output: '{ type: "array", minItems: 2, maxItems: 2, items: [{ type: "string", const: "bar" }, { type: "number" }] }'
  draft-2020-12:
    input: '{ type: "array", minItems: 2, maxItems: 2, prefixItems: [{ type: "string", const: "bar" }, { type: "number" }], $schema: "https://json-schema.org/draft/2020-12/schema" }'
    output: '{ type: "array", minItems: 2, maxItems: 2, prefixItems: [{ type: "string", const: "bar" }, { type: "number" }], $schema: "https://json-schema.org/draft/2020-12/schema" }'
  openapi-3.0:
    input: '{ type: "array", minItems: 2, maxItems: 2, items: { anyOf: [{ type: "string", enum: ["bar"] }, { type: "number" }] } }'
    fromInputType: '[number | "bar", number | "bar"]'
    output: '{ type: "array", minItems: 2, maxItems: 2, items: { anyOf: [{ type: "string", enum: ["bar"] }, { type: "number" }] } }'
    fromOutputType: '[number | "bar", number | "bar"]'
isEqual: (a,b)=>a===b||a[1]===b[1]
vs:
  zod: z.tuple([z.literal("bar"), z.number()])
operations:
  parse:
    expression: i=>{Array.isArray(i)&&i.length===2||e[2](i);let v0=i[0],v1=i[1];v0==="bar"||e[0](v0);typeof v1==="number"&&v1==v1||e[1](v1);return i}
    examples:
      valid:
        input: '["bar", 42]'
        output: '["bar", 42]'
      wrong-length:
        input: '["bar"]'
        error: Expected ["bar", number], received ["bar"]
      invalid-literal:
        input: '["baz", 42]'
        error: 'Failed at [0]: Expected "bar", received "baz"'
      invalid-item:
        input: '["bar", "not a number"]'
        error: 'Failed at [1]: Expected number, received "not a number"'
  decode: identity
  encode: identity
