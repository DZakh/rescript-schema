# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.union([S.number, S.string]))
  input: string
  output: string | number
  instantiations: 5373
jsonSchema:
  input: '{ type: "string" }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from string to number | string: string has the same type as the source and the others don't. Use S.to on that arm, or S.never to mark it unreachable"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous conversion from number | string to string: string has the same type as the target and the others don't. Use S.to on that arm, or S.never to mark it unreachable"
