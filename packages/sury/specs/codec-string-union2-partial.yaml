# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.to, S.union([S.number, S.string]))
  input: string
  output: string | number
  instantiations: 5373
jsonSchema:
  input: '{ type: "string" }'
  output: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from string to number | string: the string arm matches as it is. Use S.to for a coder, or S.never to drop it"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous conversion from number | string to string: the string arm matches as it is. Use S.to for a coder, or S.never to drop it"
