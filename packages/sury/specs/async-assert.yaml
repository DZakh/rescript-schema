# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.string.with(S.to, S.string, { decode: { async: async (value) => { if (value === "bad") { throw new Error("rejected") } return value } }, encode: "auto" })'
  input: string
  output: string
  instantiations: 4451
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "string" }'
vs:
  zod: z.string().refine(async (value) => value !== "bad")
operations:
  parse:
    isAsync: true
    expression: i=>{typeof i==="string"||e[3](i);let v0;try{v0=e[0](i).catch(x=>e[1](x))}catch(x){e[1](x)}return v0.then(v0=>{typeof v0==="string"||e[2](v0);return v0})}
    examples:
      valid:
        input: '"hello"'
        output: '"hello"'
      rejected-by-assert:
        input: '"bad"'
        error: rejected
      invalid-type:
        input: "42"
        error: Expected string, received 42
  decode:
    isAsync: true
    expression: i=>{let v0;try{v0=e[0](i).catch(x=>e[1](x))}catch(x){e[1](x)}return v0.then(v0=>{typeof v0==="string"||e[2](v0);return v0})}
    examples:
      valid:
        input: '"hello"'
        output: '"hello"'
      rejected-by-assert:
        input: '"bad"'
        error: rejected
  encode: identity
