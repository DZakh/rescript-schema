# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.schema(true)
  aliases:
    - S.literal(true)
  input: "true"
  output: "true"
  instantiations: 378
jsonSchema:
  input: '{ type: "boolean", const: true }'
  output: '{ type: "boolean", const: true }'
  openapi-3.0:
    input: '{ type: "boolean", enum: [true] }'
    output: '{ type: "boolean", enum: [true] }'
isEqual: alwaysEqual
vs:
  zod: z.literal(true)
operations:
  parse:
    expression: i=>{i===true||e[0](i);return i}
    examples:
      valid:
        input: "true"
        output: "true"
      invalid-value:
        input: "false"
        error: Expected true, received false
      invalid-type:
        input: "0"
        error: Expected true, received 0
  decode: eq-to-parse
  encode: eq-to-parse
