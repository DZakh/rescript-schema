# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.schema([S.string.with(S.to, S.number, (s) => Number(s))])
  input: "[string]"
  output: "[number]"
  instantiations: 5157
jsonSchema:
  input: '{ type: "array", minItems: 1, maxItems: 1, items: [{ type: "string" }] }'
  output: '{ type: "array", minItems: 1, maxItems: 1, items: [{ type: "number" }] }'
  draft-2020-12:
    input: '{ type: "array", minItems: 1, maxItems: 1, prefixItems: [{ type: "string" }], $schema: "https://json-schema.org/draft/2020-12/schema" }'
    output: '{ type: "array", minItems: 1, maxItems: 1, prefixItems: [{ type: "number" }], $schema: "https://json-schema.org/draft/2020-12/schema" }'
  openapi-3.0:
    input: '{ type: "array", minItems: 1, maxItems: 1, items: { anyOf: [{ type: "string" }] } }'
    output: '{ type: "array", minItems: 1, maxItems: 1, items: { anyOf: [{ type: "number" }] } }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{Array.isArray(i)&&i.length===1||e[4](i);let v1=i["0"];typeof v1==="string"||e[3](v1);let v0;try{v0=e[0](v1)}catch(x){e[1](x)}typeof v0==="number"&&v0===v0||e[2](v0);return [v0]}
    examples:
      valid:
        input: '["123"]'
        output: "[123]"
      not-numeric-text:
        input: '["abc"]'
        error: "Failed at [0]: Expected number, received NaN"
      not-a-string-item:
        input: "[123]"
        error: "Failed at [0]: Expected string, received 123"
  decode:
    expression: i=>{let v0;try{v0=e[0](i["0"])}catch(x){e[1](x)}typeof v0==="number"&&v0===v0||e[2](v0);return [v0]}
    examples:
      valid:
        input: '["123"]'
        output: "[123]"
  encode:
    creationError: "SuryError: Failed at [0]: Encoding is ambiguous when only a decode function is provided. Use S.to(target, {decode, encode})"
