# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.to(S.array(S.string), S.array(S.unknown)).with(S.length, 2)
  input: string[]
  output: "[unknown, unknown]"
  instantiations: 1414
jsonSchema:
  input: '{ items: { type: "string" }, type: "array" }'
  output: '{ items: {}, type: "array", minItems: 2, maxItems: 2 }'
  fromOutputType: "[JSON, JSON]"
isEqual:
  input: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(a[i]===b[i]))return false;return true}
  output: (a,b)=>{if(a===b)return true;let n=a.length;if(n!==b.length)return false;for(let i=0;i<n;i++)if(!(e[0](a[i],b[i])))return false;return true}
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{Array.isArray(i)||e[2](i);for(let v0=0;v0<i.length;++v0){try{let v1=i[v0];typeof v1==="string"||e[0](v1);}catch(v2){v2.path=[v0,...v2.path];throw v2}}i.length===2||e[1](i);return i}
    examples:
      at bound:
        input: '["a", "b"]'
        output: '["a", "b"]'
      under bound:
        input: '["a"]'
        error: Expected unknown[].length == 2, received ["a"]
      over bound:
        input: '["a", "b", "c"]'
        error: Expected unknown[].length == 2, received ["a", "b", "c"]
      empty:
        input: "[]"
        error: Expected unknown[].length == 2, received []
      element type still checked:
        input: '["a", 1]'
        error: "Failed at [1]: Expected string, received 1"
  decode:
    expression: i=>{i.length===2||e[0](i);return i}
    examples:
      at bound:
        input: '["a", "b"]'
        output: '["a", "b"]'
      under bound:
        input: '["a"]'
        error: Expected unknown[].length == 2, received ["a"]
  encode:
    expression: i=>{i.length===2||e[1](i);for(let v0=0;v0<i.length;++v0){try{let v1=i[v0];typeof v1==="string"||e[0](v1);}catch(v2){v2.path=[v0,...v2.path];throw v2}}return i}
    examples:
      at bound:
        input: '["a", "b"]'
        output: '["a", "b"]'
      under bound:
        input: '["a"]'
        error: Expected unknown[].length == 2, received ["a"]
