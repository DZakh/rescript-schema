# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.string).with(S.to, S.nullable(S.string))
  input: string | undefined
  output: string | null
  instantiations: 4728
jsonSchema:
  input: Expected JSON, received string | undefined
  output: '{ anyOf: [{ type: "string" }, { type: "null" }] }'
  openapi-3.0:
    output: '{ type: "string", nullable: true }'
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{for(;;){if(typeof i==="string")break;if(i===void 0){i=null;break}e[0](i)}return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
        divergence:
          reason: the checks compile the reverse direction, which this codec leaves ambiguous, so they raise on the schema instead of answering about the value
          check: Ambiguous string -> string | null. Should null be decoded or ignored? Choose with S.to for string -> null, or S.never -> null
      undefined-bridges-to-null:
        input: undefined
        output: "null"
        divergence:
          reason: the checks compile the reverse direction, which this codec leaves ambiguous, so they raise on the schema instead of answering about the value
          check: Ambiguous string -> string | null. Should null be decoded or ignored? Choose with S.to for string -> null, or S.never -> null
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){if(typeof i==="string")break;if(i===null){i=void 0;break}e[0](i)}return i}
    examples:
      string:
        input: '"abc"'
        output: '"abc"'
      null-bridges-to-undefined:
        input: "null"
        output: undefined
