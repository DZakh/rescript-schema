# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.optional(S.string))
  input: string
  output: string | undefined
  instantiations: 4599
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received string | undefined
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[0](i);i=i||void 0;return i}
    examples:
      set:
        input: '"x"'
        output: '"x"'
      blank:
        input: '""'
        output: undefined
      unset:
        input: undefined
        error: Expected env, received undefined
      number:
        input: "1"
        error: Expected env, received 1
  decode:
    expression: i=>{i=i||void 0;return i}
    examples:
      set:
        input: '"x"'
        output: '"x"'
      blank:
        input: '""'
        output: undefined
  encode:
    expression: i=>{for(;;){if(typeof i==="string")break;if(i===void 0){i="";break}e[0](i)}return i}
    examples:
      set:
        input: '"x"'
        output: '"x"'
      absent:
        input: undefined
        output: '""'
