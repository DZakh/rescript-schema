# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string.with(S.minLength, 5), S.string.with(S.to, S.number), S.string.with(S.maxLength, 1)])
  input: string
  output: string | number
  instantiations: 5955
jsonSchema:
  input: '{ anyOf: [{ type: "string", minLength: 5 }, { type: "string" }, { type: "string", maxLength: 1 }] }'
  output: '{ anyOf: [{ type: "string", minLength: 5 }, { type: "number" }, { type: "string", maxLength: 1 }] }'
vs:
  zod: z.union([z.string().min(5), z.string().pipe(z.coerce.number()), z.string().max(1)])
operations:
  parse:
    expression: i=>{for(;;){let r;if(typeof i==="string"){try{i.length>9||e[0](i)>4||e[1](i);break}catch(x){(r||(r=[])).push(e[5](x))}try{let v0=+i;v0===v0&&(v0||i.trim())||e[2](i);i=v0;break}catch(x){(r||(r=[])).push(e[5](x))}try{i.length<2||e[3](i)<2||e[4](i);break}catch(x){(r||(r=[])).push(e[5](x))}}e[6](i,...(r||[]))}return i}
    examples:
      validation-wins-before-coercion:
        input: '"alphabet"'
        output: '"alphabet"'
      coercing-middle-member:
        input: '"123"'
        output: "123"
      validation-fallback-after-coercion:
        input: '"x"'
        output: '"x"'
      rejected-by-all:
        input: '"bad"'
        error: |-
          Expected string.length >= 5 | string | string.length <= 1, received "bad"
          - Expected string.length >= 5, received "bad"
          - Expected number, received "bad"
          - Expected string.length <= 1, received "bad"
      wrong-tag:
        input: "true"
        error: Expected string.length >= 5 | string | string.length <= 1, received true
  decode: eq-to-parse
  encode:
    expression: i=>{for(;;){if(typeof i==="string"){for(;;){let r;try{i.length>9||e[0](i)>4||e[1](i);break}catch(x){(r||(r=[])).push(e[4](x))}try{i.length<2||e[2](i)<2||e[3](i);break}catch(x){(r||(r=[])).push(e[4](x))}e[5](i,...(r||[]))};break}if(typeof i==="number"&&i===i){i=""+i;break}e[6](i)}return i}
    examples:
      validation-first:
        input: '"alphabet"'
        output: '"alphabet"'
      number:
        input: "123"
        output: '"123"'
      validation-fallback:
        input: '"x"'
        output: '"x"'
      rejected-string:
        input: '"bad"'
        error: |-
          Expected string.length >= 5 | number | string.length <= 1, received "bad"
          - Expected string.length >= 5, received "bad"
          - Expected string.length <= 1, received "bad"
