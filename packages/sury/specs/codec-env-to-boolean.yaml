# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.boolean)
  input: string
  output: boolean
  instantiations: 4419
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "boolean" }'
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[2](i);typeof i==="string"||e[0](i);let v0;(v0=i==="true")||i==="false"||e[1](i);return v0}
    examples:
      "true":
        input: '"true"'
        output: "true"
      "false":
        input: '"false"'
        output: "false"
      on:
        input: '"on"'
        error: Expected boolean, received "on"
      yes:
        input: '"yes"'
        error: Expected boolean, received "yes"
      one:
        input: '"1"'
        error: Expected boolean, received "1"
      blank:
        input: '""'
        error: Expected boolean, received ""
  decode:
    expression: i=>{typeof i==="string"||e[0](i);let v0;(v0=i==="true")||i==="false"||e[1](i);return v0}
    examples:
      "true":
        input: '"true"'
        output: "true"
  encode:
    expression: i=>{return ""+i}
    examples:
      "true":
        input: "true"
        output: '"true"'
      "false":
        input: "false"
        output: '"false"'
