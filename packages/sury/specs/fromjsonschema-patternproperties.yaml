# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: 'S.fromJSONSchemaOrThrow({ patternProperties: { "f.*o": { type: "integer" } } })'
  input: JSON
  output: JSON
  instantiations: 593
jsonSchema:
  input: '{ patternProperties: { "f.*o": { type: "integer" } } }'
  output: '{ patternProperties: { "f.*o": { type: "integer" } } }'
isEqual: (a,b)=>a===b||e[0](a,b)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{e[0](i);e[1](i)||e[2](i);return i}
    examples:
      matching-integer:
        input: "{ foo: 1 }"
        output: "{ foo: 1 }"
      matching-string:
        input: '{ foo: "bar" }'
        error: Should pass every matching patternProperties schema.
      unmatched-key:
        input: '{ bar: "x" }'
        output: '{ bar: "x" }'
      ignores-array:
        input: '["foo"]'
        output: '["foo"]'
  decode:
    expression: i=>{e[0](i)||e[1](i);return i}
    examples:
      matching-integer:
        input: "{ foo: 1 }"
        output: "{ foo: 1 }"
  encode:
    expression: i=>{e[0](i)||e[1](i);return i}
    examples:
      matching-integer:
        input: "{ foo: 1 }"
        output: "{ foo: 1 }"
