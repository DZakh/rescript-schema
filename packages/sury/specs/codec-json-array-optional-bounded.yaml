# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.json.with(S.to, S.array(S.optional(S.number.with(S.lte, 1))))
  input: JSON
  output: (number | undefined)[]
  instantiations: 5347
# FIXME: jsonSchema.input drops the item's `maximum: 1` — a variant converted
# through `.to(json)` reports the target's type without the source's refinements.
# The generated code still enforces the bound, in both directions.
jsonSchema:
  input: '{ items: { anyOf: [{ type: "number" }, { type: "null" }] }, type: "array" }'
  fromInputType: (number | null)[]
  output: "Failed at []: Expected JSON, received number <= 1 | undefined"
  openapi-3.0:
    input: '{ items: { type: "number", nullable: true }, type: "array" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{Array.isArray(i)||e[2](i);let v3=new Array(i.length);for(let v0=0;v0<i.length;++v0){try{let v1=i[v0];for(;;){if(typeof v1==="number"&&v1===v1){v1<=1||e[0](v1);break}if(v1===null){v1=void 0;break}e[1](v1)}v3[v0]=v1}catch(v2){v2.path=[v0,...v2.path];throw v2}}return v3}
    examples:
      null-becomes-undefined:
        input: "[null, 1]"
        output: "[undefined, 1]"
      bound-still-enforced:
        input: "[2]"
        error: "Failed at [0]: Expected number <= 1, received 2"
      empty:
        input: "[]"
        output: "[]"
  decode: eq-to-parse
  encode:
    expression: i=>{let v4=new Array(i.length);for(let v1=0;v1<i.length;++v1){try{let v2=i[v1];for(;;){if(typeof v2==="number"&&v2===v2){v2<=1||e[0](v2);break}if(v2===void 0){v2=null;break}e[1](v2)}v4[v1]=v2}catch(v3){v3.path=[v1,...v3.path];throw v3}}return v4}
    examples:
      undefined-becomes-null:
        input: "[undefined, 1]"
        output: "[null, 1]"
      bound-still-enforced:
        input: "[2]"
        error: "Failed at [0]: Expected number <= 1, received 2"
