# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.string, S.union([S.number, S.boolean])]).with(S.to, S.union([S.boolean, S.number, S.string]))
  input: string | number | boolean
  output: string | number | boolean
  instantiations: 6221
jsonSchema:
  input: '{ anyOf: [{ type: "boolean" }, { type: "number" }, { type: "string" }] }'
  output: '{ anyOf: [{ type: "string" }, { type: "number" }, { type: "boolean" }] }'
vs:
  zod: z.union([z.string(), z.union([z.number(), z.boolean()])])
operations:
  parse:
    expression: i=>{(typeof i==="string"||typeof i==="number"&&i==i||typeof i==="boolean")||e[0](i);return i}
    examples:
      string:
        input: '"a"'
        output: '"a"'
        divergence:
          reason: the checks compile the reverse direction, which this codec leaves ambiguous, so they raise on the schema instead of answering about the value
          check: Ambiguous string -> boolean | number | string. Should boolean be decoded or ignored? Choose with S.to for string -> boolean, or S.never -> boolean
      number:
        input: "1"
        output: "1"
        divergence:
          reason: the checks compile the reverse direction, which this codec leaves ambiguous, so they raise on the schema instead of answering about the value
          check: Ambiguous string -> boolean | number | string. Should boolean be decoded or ignored? Choose with S.to for string -> boolean, or S.never -> boolean
      boolean:
        input: "true"
        output: "true"
        divergence:
          reason: the checks compile the reverse direction, which this codec leaves ambiguous, so they raise on the schema instead of answering about the value
          check: Ambiguous string -> boolean | number | string. Should boolean be decoded or ignored? Choose with S.to for string -> boolean, or S.never -> boolean
      nan-rejected:
        input: NaN
        error: Expected string | number | boolean, received NaN
  decode: eq-to-parse
  encode:
    expression: i=>{(typeof i==="boolean"||typeof i==="number"&&i==i||typeof i==="string")||e[0](i);return i}
    examples:
      string:
        input: '"a"'
        output: '"a"'
      number:
        input: "1"
        output: "1"
      boolean:
        input: "true"
        output: "true"
