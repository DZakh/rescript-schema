# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.urlSearchParams.with(S.to, S.schema({ tags: S.array(S.optional(S.string)) }))"
  input: URLSearchParams
  output: "{ tags: (string | undefined)[]; }"
  instantiations: 5954
jsonSchema:
  input: Expected JSON, received URLSearchParams
  output: "Failed at tags[]: Expected JSON, received string | undefined"
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: (a,b)=>a===b||e[0](a.tags,b.tags)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Failed at tags: Can't decode search param -> (string | undefined)[]. A repeated key is positional, so every item needs an entry"
  decode: eq-to-parse
  encode: eq-to-parse
