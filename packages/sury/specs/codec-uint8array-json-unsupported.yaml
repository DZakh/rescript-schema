# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.json)
  input: Uint8Array<ArrayBufferLike>
  output: JSON
  instantiations: 4419
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: "{}"
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode Uint8Array to JSON. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode: eq-to-parse
