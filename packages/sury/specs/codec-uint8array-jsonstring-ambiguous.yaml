# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.jsonString)
  input: Uint8Array<ArrayBufferLike>
  output: string
  instantiations: 4419
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: '{ type: "string", contentMediaType: "application/json" }'
  openapi-3.0:
    output: '{ type: "string" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Ambiguous conversion from Uint8Array to JSON string. Use S.to(from, to, "unpack" | "pack")'
  decode: eq-to-parse
  encode: eq-to-parse
