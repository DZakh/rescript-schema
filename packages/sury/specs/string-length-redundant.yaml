# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.string.with(S.length, 2).with(S.length, 2, "MY MESSAGE")
  input: string
  output: string
  instantiations: 1085
jsonSchema:
  input: '{ type: "string", minLength: 2, maxLength: 2 }'
  output: '{ type: "string", minLength: 2, maxLength: 2 }'
vs:
  zod: z.string().length(2).length(2, "MY MESSAGE")
operations:
  parse:
    expression: i=>{typeof i==="string"||e[2](i);i.length>1||e[0](i);i.length<3||e[1](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-short:
        input: '"a"'
        error: MY MESSAGE
      too-long:
        input: '"abc"'
        error: Expected string.length == 2, received "abc"
      empty:
        input: '""'
        error: MY MESSAGE
  decode:
    expression: i=>{i.length>1||e[0](i);i.length<3||e[1](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-long:
        input: '"abc"'
        error: Expected string.length == 2, received "abc"
  encode:
    expression: i=>{i.length>1||e[0](i);i.length<3||e[1](i);return i}
    examples:
      at-the-length:
        input: '"ab"'
        output: '"ab"'
      too-long:
        input: '"abc"'
        error: Expected string.length == 2, received "abc"
