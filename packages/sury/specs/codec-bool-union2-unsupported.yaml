# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.boolean.with(S.to, S.union([S.string, S.symbol]))
  input: boolean
  output: string | symbol
  instantiations: 5394
jsonSchema:
  input: '{ type: "boolean" }'
  output: Expected JSON, received string | symbol
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode boolean to symbol. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode symbol to boolean. Use S.to to define a custom decoder"
