# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.formData.with(S.to, S.number)
  input: FormData
  output: number
  instantiations: 4430
jsonSchema:
  input: Expected JSON, received FormData
  output: '{ type: "number" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode FormData to number. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode number to FormData. Use S.to to define a custom decoder"
