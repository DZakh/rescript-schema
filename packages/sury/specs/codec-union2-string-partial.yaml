# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.number, S.string]).with(S.to, S.string)
  input: string | number
  output: string
  instantiations: 5079
jsonSchema:
  input: '{ anyOf: [{ type: "number" }, { type: "string" }] }'
  output: '{ type: "string" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Ambiguous conversion from number | string to string. Use S.to(from, to, {decode, encode}), or S.never on an arm"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Ambiguous conversion from string to number | string. Use S.to(from, to, {decode, encode}), or S.never on an arm"
