# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union(["foo", 123, true])
  input: true | "foo" | 123
  output: true | "foo" | 123
  instantiations: 617
jsonSchema:
  input: '{ enum: ["foo", 123, true] }'
  output: '{ enum: ["foo", 123, true] }'
isEqual: strictEqual
vs:
  zod: z.union([z.literal("foo"), z.literal(123), z.literal(true)])
operations:
  parse:
    expression: i=>{(typeof i==="string"&&i==="foo"||typeof i==="number"&&i==i&&i===123||typeof i==="boolean"&&i===true)||e[0](i);return i}
    examples:
      string:
        input: '"foo"'
        output: '"foo"'
      number:
        input: "123"
        output: "123"
      boolean:
        input: "true"
        output: "true"
      invalid:
        input: '"bar"'
        error: Expected "foo" | 123 | true, received "bar"
  decode: identity
  encode: identity
