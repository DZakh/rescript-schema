# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.number.with(S.to, S.string).with(S.minLength, 2)
  input: number
  output: string
  instantiations: 4914
jsonSchema:
  input: '{ type: "number" }'
  output: '{ type: "string" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="number"&&i===i||e[1](i);let v0=""+i;v0.length>1||e[0](v0);return v0}
    examples:
      long-enough:
        input: "42"
        output: '"42"'
      too-short:
        input: "4"
        error: Expected string.length >= 2, received "4"
  decode:
    expression: i=>{let v0=""+i;v0.length>1||e[0](v0);return v0}
    examples:
      long-enough:
        input: "42"
        output: '"42"'
  encode:
    expression: i=>{i.length>1||e[1](i);let v0=+i;v0===v0&&(v0||i.trim())||e[0](i);return v0}
    examples:
      long-enough:
        input: '"42"'
        output: "42"
      too-short:
        input: '"4"'
        error: Expected string.length >= 2, received "4"
