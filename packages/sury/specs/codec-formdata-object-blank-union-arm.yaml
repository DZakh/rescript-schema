# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.formData.with(S.to, S.schema({ a: S.union([S.string, S.number]) }))"
  input: FormData
  output: "{ a: string | number; }"
  instantiations: 6208
jsonSchema:
  input: Expected JSON, received FormData
  output: '{ type: "object", properties: { a: { anyOf: [{ type: "string" }, { type: "number" }] } }, required: ["a"] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: 'SuryError: Failed at a: Ambiguous "" for string | number: a blank input is a value or a missing one. Use S.nonEmpty, S.minLength(0), S.optional or S.nullable to say which'
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Failed at a: Ambiguous conversion from string | number to string: string has the same type as the target and the others don't. Use S.to on that arm, or S.never to mark it unreachable"
