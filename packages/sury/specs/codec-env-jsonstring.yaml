# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.record(S.env).with(S.to, S.schema({ FLAGS: S.jsonString.with(S.to, S.schema({ debug: S.boolean })) }))"
  input: "{ [x: string]: string | undefined; }"
  output: "{ FLAGS: { debug: boolean; }; }"
  instantiations: 6676
jsonSchema:
  input: '{ type: "object", additionalProperties: { type: "string" } }'
  fromInputType: "{ [key: string]: string; }"
  output: '{ type: "object", properties: { FLAGS: { type: "object", properties: { debug: { type: "boolean" } }, required: ["debug"] } }, required: ["FLAGS"] }'
isEqual:
  input: (a,b)=>{if(a===b)return true;let n=0;for(const k in a){if(!(k in b)||!(a[k]===b[k]))return false;n++}for(const k in b)n--;return !n}
  output: (a,b)=>a===b||a.FLAGS.debug===b.FLAGS.debug
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="object"&&i&&!Array.isArray(i)||e[6](i);for(let v0 in i){try{let v1=i[v0];typeof v1==="string"||e[0](v1);}catch(v2){v2.path=[v0,...v2.path];throw v2}}let v3=i.FLAGS;if(v3!==void 0){typeof v3==="string"||e[4](v3);let v4;try{v4=JSON.parse(v3)}catch(t){e[1](v3)}typeof v4==="object"&&v4&&!Array.isArray(v4)||e[3](v4);let v5=v4.debug;typeof v5==="boolean"||e[2](v5);v3={debug:v5};}else{e[5](v3)}return {FLAGS:v3}}
    examples:
      nested-document:
        input: '{ FLAGS: "{\"debug\":true}" }'
        output: "{ FLAGS: { debug: true } }"
      not-json:
        input: '{ FLAGS: "nope" }'
        error: 'Failed at FLAGS: Expected JSON string, received "nope"'
      missing:
        input: "{}"
        error: "Failed at FLAGS: Expected JSON string, received undefined"
  decode:
    expression: i=>{let v1=i.FLAGS;if(v1!==void 0){typeof v1==="string"||e[3](v1);let v2;try{v2=JSON.parse(v1)}catch(t){e[0](v1)}typeof v2==="object"&&v2&&!Array.isArray(v2)||e[2](v2);let v3=v2.debug;typeof v3==="boolean"||e[1](v3);v1={debug:v3};}else{e[4](v1)}return {FLAGS:v1}}
    examples:
      nested-document:
        input: '{ FLAGS: "{\"debug\":true}" }'
        output: "{ FLAGS: { debug: true } }"
  encode:
    expression: i=>{let v0=i.FLAGS;return {FLAGS:"{\"debug\":"+v0.debug+"}"}}
    examples:
      built:
        input: "{ FLAGS: { debug: true } }"
        output: '{ FLAGS: "{\"debug\":true}" }'
