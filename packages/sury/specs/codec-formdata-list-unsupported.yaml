# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.formData.with(S.to, S.schema({ tags: S.array(S.optional(S.string)) }))"
  input: FormData
  output: "{ tags: (string | undefined)[]; }"
  instantiations: 5952
jsonSchema:
  input: Expected JSON, received FormData
  output: "Failed at tags[]: Expected JSON, received string | undefined"
isEqual:
  input: (a,b)=>a===b||e[0](a,b)
  output: (a,b)=>a===b||e[0](a.tags,b.tags)
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Failed at tags: Can't decode form field -> (string | undefined)[]. A repeated key is positional, so every item needs an entry"
  decode: eq-to-parse
  encode: eq-to-parse
