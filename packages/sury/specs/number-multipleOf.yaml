# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.number.with(S.multipleOf, 2)
  aliases:
    - S.number.with(S.multipleOf, 2).with(S.multipleOf, 2)
  input: number
  output: number
  instantiations: 578
jsonSchema:
  input: '{ type: "number", multipleOf: 2 }'
  output: '{ type: "number", multipleOf: 2 }'
vs:
  zod: z.number().multipleOf(2)
operations:
  parse:
    expression: i=>{typeof i==="number"&&i==i||e[1](i);i%2==0||e[0](i);return i}
    examples:
      valid:
        input: "4"
        output: "4"
      valid-zero:
        input: "0"
        output: "0"
      valid-negative:
        input: "-4"
        output: "-4"
      invalid-odd:
        input: "3"
        error: Expected number % 2, received 3
      invalid-fraction:
        input: "2.5"
        error: Expected number % 2, received 2.5
      invalid-infinity:
        input: Infinity
        error: Expected number % 2, received Infinity
      invalid-nan:
        input: NaN
        error: Expected number % 2, received NaN
      invalid-string:
        input: '"4"'
        error: Expected number % 2, received "4"
  decode:
    expression: i=>{i%2==0||e[0](i);return i}
    examples:
      valid:
        input: "4"
        output: "4"
      invalid-odd:
        input: "3"
        error: Expected number % 2, received 3
  encode:
    expression: i=>{i%2==0||e[0](i);return i}
    examples:
      valid:
        input: "4"
        output: "4"
      invalid-odd:
        input: "3"
        error: Expected number % 2, received 3
