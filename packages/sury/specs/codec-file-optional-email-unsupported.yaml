# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.file.with(S.to, S.optional(S.email))
  input: File
  output: string | undefined
  instantiations: 4632
jsonSchema:
  input: Expected JSON, received File
  output: Expected JSON, received email | undefined
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode File -> email | undefined. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode undefined -> File. Define custom codec with S.to"
