# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.file.with(S.to, S.optional(S.email))
  input: File
  output: string | undefined
  instantiations: 4632
jsonSchema:
  input: Expected JSON, received File
  output: Expected JSON, received email | undefined
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode File to email | undefined. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode undefined to File. Use S.to to define a custom decoder"
