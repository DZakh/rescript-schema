# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.formData.with(S.to, S.schema({ user: S.schema({ name: S.string }) }))"
  input: FormData
  output: "{ user: { name: string; }; }"
  instantiations: 6364
jsonSchema:
  input: Expected JSON, received FormData
  output: '{ type: "object", properties: { user: { type: "object", properties: { name: { type: "string" } }, required: ["name"] } }, required: ["user"] }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Failed at user: Can't decode form field to { name: string; }. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Failed at user: Can't decode { name: string; } to FormData. Use S.to to define a custom decoder"
