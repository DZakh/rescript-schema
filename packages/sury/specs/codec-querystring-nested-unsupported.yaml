# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: "S.queryString.with(S.to, S.schema({ user: S.schema({ name: S.string }) }))"
  input: string
  output: "{ user: { name: string; }; }"
  instantiations: 6465
jsonSchema:
  input: '{ type: "string" }'
  output: '{ type: "object", properties: { user: { type: "object", properties: { name: { type: "string" } }, required: ["name"] } }, required: ["user"] }'
isEqual:
  input: strictEqual
  output: (a,b)=>a===b||a.user.name===b.user.name
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Failed at user: Can't decode search param -> { name: string; }. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Failed at user: Can't decode { name: string; } -> URLSearchParams. Define custom codec with S.to"
