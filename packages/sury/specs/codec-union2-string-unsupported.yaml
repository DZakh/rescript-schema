# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.union([S.boolean, S.symbol]).with(S.to, S.string)
  input: boolean | symbol
  output: string
  instantiations: 5137
jsonSchema:
  input: Expected JSON, received boolean | symbol
  output: '{ type: "string" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode symbol to string. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode string to symbol. Use S.to to define a custom decoder"
