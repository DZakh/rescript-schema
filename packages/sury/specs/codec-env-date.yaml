# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.date)
  input: string
  output: Date
  instantiations: 4419
jsonSchema:
  input: Expected JSON, received env
  output: Expected JSON, received Date
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{typeof i==="string"||e[2](i);typeof i==="string"||e[0](i);let v0=new Date(i);!Number.isNaN(v0.getTime())||e[1](v0);return v0}
    examples:
      valid:
        input: '"2024-01-01T00:00:00.000Z"'
        output: new Date("2024-01-01T00:00:00.000Z")
      invalid:
        input: '"nope"'
        error: Expected Date, received invalid Date
      blank:
        input: '""'
        error: Expected Date, received invalid Date
  decode:
    expression: i=>{typeof i==="string"||e[0](i);let v0=new Date(i);!Number.isNaN(v0.getTime())||e[1](v0);return v0}
    examples:
      valid:
        input: '"2024-01-01T00:00:00.000Z"'
        output: new Date("2024-01-01T00:00:00.000Z")
  encode:
    creationError: "SuryError: Can't decode Date -> env. Define custom codec with S.to"
