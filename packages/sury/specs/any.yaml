# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.any
  input: any
  output: any
  instantiations: 254
jsonSchema:
  input: "{}"
  output: "{}"
vs:
  zod: z.any()
operations:
  parse: identity
  decode: identity
  encode: identity
