# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.optional(S.uint8Array).with(S.to, S.jsonString)
  input: Uint8Array<ArrayBufferLike> | undefined
  output: string
  instantiations: 4587
jsonSchema:
  input: Expected JSON, received Uint8Array | undefined
  output: '{ type: "string", contentMediaType: "application/json" }'
  openapi-3.0:
    output: '{ type: "string" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode Uint8Array | undefined -> JSON string. Define custom codec with S.to"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode JSON string -> Uint8Array | undefined. Define custom codec with S.to"
