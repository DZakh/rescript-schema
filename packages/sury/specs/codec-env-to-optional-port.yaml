# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.env.with(S.to, S.optional(S.port))
  input: string
  output: number | undefined
  instantiations: 4620
jsonSchema:
  input: '{ type: "string" }'
  output: Expected JSON, received port | undefined
isEqual: strictEqual
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    expression: i=>{i=i||void 0;if(i){let v0=+i;v0==v0&&(v0||i.trim())||e[1](i);v0>=0&&v0<65536&&v0%1==0||e[0](v0);i=v0;}return i}
    examples:
      valid:
        input: '"8080"'
        output: "8080"
      blank:
        input: '""'
        output: undefined
      not-a-number:
        input: '"x"'
        error: Expected port, received "x"
  decode: eq-to-parse
  encode:
    expression: i=>{if(typeof i==="number"&&i==i){i>=0&&i<65536&&i%1==0||e[0](i);i=""+i}else{e[1](i)}return i}
    examples:
      valid:
        input: "8080"
        output: '"8080"'
      # FIXME: parse reads "" as undefined, but the absent arm has no env
      # text to encode back to, so the value parse produced does not round-trip.
      absent:
        input: undefined
        error: Expected port, received undefined
