# yaml-language-server: $schema=./spec.schema.json
ts:
  schema: S.uint8Array.with(S.to, S.number)
  input: Uint8Array<ArrayBufferLike>
  output: number
  instantiations: 4419
jsonSchema:
  input: Expected JSON, received Uint8Array
  output: '{ type: "number" }'
vs:
  zod:
    _skip: not-applicable
operations:
  parse:
    creationError: "SuryError: Can't decode Uint8Array to number. Use S.to to define a custom decoder"
  decode: eq-to-parse
  encode:
    creationError: "SuryError: Can't decode number to Uint8Array. Use S.to to define a custom decoder"
